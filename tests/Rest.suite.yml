actor: RestTester
suite_namespace: App\Tests\Rest
modules:
    enabled:
        - Asserts
        - Symfony:
            app_path: 'src'
            environment: 'test'
        - REST:
            depends: Symfony
        - Doctrine:
            depends: Symfony
            cleanup: true
